require 'net/http'
require 'rexml/document'
require 'json'

module BattleNetHelper
  

  def locale(l='us')
    @locale = l
  end
  
  def realm(r)
    @realm = r
  end

  def character(c)
    @character = c
  end
  
  def path(p='battle.net')
    @path = p
  end
  
end