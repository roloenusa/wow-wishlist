<%= form_for current_user.relationships.find_by_character_id(@character), 
    :html => {:method => :delete},
    :remote => true do |f| %> 
    <div class="actions"><%= f.submit "Delete" %></div>
<% end %>