<%= form_for current_user.relationships.build(:character_id => @character.id), 
    :remote => true do |f| %> 
    <div><%= f.hidden_field :character_id %></div>
    <div class="actions"><%= f.submit "Claim" %></div>
<% end %>